<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gestion Stock - <%= typeof title !== 'undefined' ? title : '' %></title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <script src="/js/validation.js" defer></script>
</head>
<body>

    <% if (typeof userRole !== 'undefined' && userRole) { %>
    <nav>
        <div>
            <strong>Gestion Stock</strong>
        </div>
        <div>
            <span style="margin-right: 20px;">Bienvenue, <%= typeof username !== 'undefined' ? username : '' %> (<%= userRole %>)</span>
            <a href="/auth/logout">Déconnexion</a>
        </div>
    </nav>
    <% } %>

    <div class="container">
